# dare-ni-mo-narenai-watashi-dakara-v3 修正复核

基准 `frames.json` SHA256：`d512f08f77f118d91b5f235d1d6d260edade12cc8c3e323b21fce19d29aab109`

## 用户指出的 0:44 问题

`l011 誰一人 誰一人 幸せにしない`

- 两张 `誰一人` 词卡：`だれいちにん / dare ichi nin` → `だれひとり / dare hitori`
- 第三字段：`不定代词` → `连语＋否定呼应（「一人」熟字训）`
- 顶部原始 caption 数据本来是 `だれひとり`，旧渲染器却从错误词卡读取了注音，因此成片显示错误。

## 其他明确读音、注音与罗马音问题

- `l006 染まれない`：`しまれない / shima re nai` → `そまれない / soma re nai`
- `l014 大逸れて`：顶部 `おおそ / oosorete` → 按冻结 QRC 唱读改为 `だいそ / daisorete`
- `l031 歪んでしまった`：`いがんでしまった` → `ゆがんでしまった`
- `l043 引き連れてさ`：补上汉字 `連` 的振假名 `つ`
- `l044 独りでも`：汉字 `独` 上方只标 `ひと`，不再重复正文已有的 `り`
- 修正 6 处把促音 `っ` 错写成 `tsu` 的罗马音：`surechigatta`、`utatte itai`、`hatte`、`oboete shimatta`、`kowashitaku natte`、`tsukutte shimau n`
- 统一重复词卡 `誰か` 的罗马音分段。
- `l017 今日も` 的第三字段补充为 `名词（熟字训）＋提示助词`。

## 拆词与文法问题

- `l037 想像の中じゃ 足りないくらいに` 当前误拆成 `想像の中｜じゃ足り｜ないくらいに`。
- 推荐改为两卡：`想像の中じゃ`／`足りないくらいに`，其中 `じゃ` 归前项，`足りないくらいに` 保持连续程度表达。
- `l018 いい` 从误标的“助词”改为“形容词”。
- 进一步补全 `わけもない`、`てみる＋たい`、`てしまう＋ん`、口语可能形（ら抜き）、`けど` 逆接、引用 `って` 等结构。
- `l009 なんて`、`l024 さ`、`l028 のかな`、`l035 って` 四张纯助词卡删除 `zhMeaning`，改用语境化 `functionZh`，并去掉旧文案里的冒号标签。

## 暂定整句中文修正

- `l007`：`以为这只是一场梦，便笑给人看` → `把这一切当作一场梦，故作笑颜`
- `l011`：`无法让任何一个人幸福` → `我不会让任何一个人幸福`
- `l024`：`即使无法成为任何人，我也能挺起胸膛` → `无法成为任何人的我，也要挺起胸膛啊`
- `l025`：`再见，再见，把不需要的一切都告别` → `再见，再见，不需要的一切`
- `l030`：`不是这个，也不是那个` → `不是那个，也不是这个`
- `l033`：`若晨曦来临，身边有人，内心便会雀跃` → `晨曦之中有人相伴，内心便会雀跃`
- `l035`：`即使困难，也要跨越过去` → `说好了，无论多么困难都要跨越过去`

## 复核结果

- 41 帧已全部检查。
- 没有片假名舶来语，也没有纯英文歌词，因此不存在 `sourceWord` 或英文排除遗漏。
- 三个角色的提案全部基于同一版 `frames.json`，旧值均可复现。
- `frames.json` 尚未修改；需要明确采纳范围后再合并并生成静态校对帧。

完整证据：

- `project/proposals/correction-lexical.json`
- `project/proposals/correction-grammar.json`
- `project/proposals/correction-translation.json`
- `project/review/correction-integration-report.json`
